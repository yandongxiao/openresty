events{}

http {
    server {
        location / {
            # set_uri类似rewrite的功能
            content_by_lua_block {
                -- ngx.req.set_uri("/foo")
                ngx.say(ngx.var.uri)
            }
        }
    }
}
